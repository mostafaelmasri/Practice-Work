//write a function that takes an array of
//number as a parameter and loops over the 
//array and adds 1 to each number in 
//the array and prints the new number.

var myArr=[1, 2, 3];

// function myfunc();

for(var i = 0; i=myArr.length; i++){
    var thing = myArr[i];
 
}